/**
  * 06 TYPE PARAMS
  *
  * Arrays, Lists etc. required type params during creation (similar to generics in java)
  */

// 06-01 array with type parameter "String"
val typeParamArray: Array[String] = Array("one", "two", "three")